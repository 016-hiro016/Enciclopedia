document.addEventListener('DOMContentLoaded',()=>{const $=s=>document.querySelector(s), content=$('#content'), log=$('#prayer-log');
$('#pray').addEventListener('click',()=>{log.textContent='ВОЗНОШУ ЛИТАНИЮ ОМНИССИИ...';anime({targets:'.prayer-box',scale:[.96,1],opacity:[.6,1],duration:900,easing:'easeOutExpo'});setTimeout(()=>{log.textContent='МАШИННЫЙ ДУХ: МОЛИТВА ПРИНЯТА // ДОСТУП РАЗРЕШЁН';gsap.to('#prayer',{opacity:0,duration:.8,onComplete:()=>$('#prayer').remove()});},1400)});
async function loadCategory(slug){document.querySelectorAll('.category').forEach(x=>x.classList.toggle('active',x.dataset.slug===slug));const data=await fetch(`/api/category/${slug}`).then(r=>r.json());$('#main-title').textContent=`${data.name} // КАТАЛОГ`;let html=`<div class="entry-head"><div class="eyebrow">ARCHIVE SECTOR // ${data.name}</div><h1>${data.name}</h1><p>${data.description}</p></div>`;for(const [group,items] of Object.entries(data.groups)){html+=`<div class="group-title">// ${group}</div>`;for(const x of items)html+=`<button class="entry-card w-100 text-start" data-id="${x.id}"><h3>${x.title}</h3><p>${x.description}</p></button>`}content.innerHTML=html;document.querySelectorAll('.entry-card').forEach(x=>x.onclick=()=>loadEntry(x.dataset.id));gsap.from('.entry-card',{y:12,opacity:0,stagger:.04,duration:.35});}
async function loadEntry(id){const x=await fetch(`/api/entry/${id}`).then(r=>r.json());content.innerHTML=`<div class="entry-head"><div class="eyebrow">${x.category} // ${x.subcategory}</div><h1>${x.title}</h1><p>${x.description}</p></div><article class="article"><p>${x.body}</p><div class="tiny">TAGS: ${x.tags.join(' • ')}</div></article>`;gsap.from('.article',{opacity:0,y:15,duration:.5})}
document.querySelectorAll('.category').forEach(x=>x.onclick=()=>loadCategory(x.dataset.slug));
let timer;$('#search').addEventListener('input',e=>{clearTimeout(timer);timer=setTimeout(async()=>{const q=e.target.value.trim();if(!q)return;const data=await fetch(`/api/search?q=${encodeURIComponent(q)}`).then(r=>r.json());$('#main-title').textContent='ПОИСК // '+q;content.innerHTML=`<div class="entry-head"><div class="eyebrow">NOOSPHERIC SEARCH</div><h1>РЕЗУЛЬТАТЫ: ${q}</h1></div>`+(data.results.length?data.results.map(x=>`<button class="entry-card w-100 text-start" data-id="${x.id}"><h3>${x.title}</h3><p>${x.category} // ${x.subcategory} — ${x.description}</p></button>`).join(''):'<p>ЗАПИСИ НЕ ОБНАРУЖЕНЫ.</p>');document.querySelectorAll('.entry-card').forEach(x=>x.onclick=()=>loadEntry(x.dataset.id))},180)});$('#clear-search').onclick=()=>{$('#search').value='';$('#search').focus()};
async function system(){const x=await fetch('/api/system').then(r=>r.json());$('#clock').textContent=x.time;$('#date').textContent=x.date;$('#imperial').textContent=x.imperial_year?`${x.imperial_year}.M41`:'999.M41';$('#load').textContent=x.load+'%';$('#loadbar').style.width=x.load+'%';$('#temp').textContent=x.temperature+' °C'}system();setInterval(system,1000);});